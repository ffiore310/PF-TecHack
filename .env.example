# Configurações do Web Security Scanner - Docker
# Copie este arquivo para .env e ajuste os valores

# ====== SEGURANÇA ======
# IMPORTANTE: Altere esta chave em produção!
# Gere uma nova: python3 -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=change-this-secret-key-in-production-use-python-secrets-module

# ====== BANCO DE DADOS ======
DATABASE_PATH=/app/src/web/scanner.db

# Para usar PostgreSQL ao invés de SQLite, descomente:
# DATABASE_URL=postgresql://scanner:scanner_password@db:5432/scanner_db

# ====== FLASK ======
FLASK_ENV=production
FLASK_DEBUG=0

# ====== PORTAS ======
# Porta externa (onde você acessa no navegador)
EXTERNAL_PORT=5001

# ====== POSTGRES (se usar) ======
POSTGRES_USER=scanner
POSTGRES_PASSWORD=scanner_password_change_in_production
POSTGRES_DB=scanner_db
